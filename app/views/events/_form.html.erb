<%= form_for(event) do |f| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
          <div class="form-group">
          <%= f.label :name %>
          <%= f.text_field :name, autofocus: true, class: "form-control" %>
          </div>
 
            <div class="form-group">
              <%= f.label :story %>
              <%= f.text_area :story, autofocus: true, class: "form-control" %>
            </div>
          
            <div class="form-group">
              <%= f.label :when %>
              <%= f.datetime_field :when, autofocus: true, class: "form-control" %>
            </div>
          
            <div class="form-group">
              <%= f.label :where %>
              <%= f.text_area :where, autofocus: true, class: "form-control" %>
            </div>
          
            <div class="form-group">
              <%= f.label :contact %>
              <%= f.text_area :contact, autofocus: true, class: "form-control" %>
            </div>
          
            <div class="field">
              <%= f.hidden_field :user_id %>
            </div>

            <div class="form-group">
            <%= f.label :avatar, class: 'control-label'  %>
          
            <%= f.file_field :avatar %>
         
              </div>
          
            <div class="actions">
               <%= f.submit "Create event", class: "btn btn-primary" %>
            </div>
<% end %>
